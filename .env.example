# Contact form endpoint (default works for Vercel api)
VITE_CONTACT_ENDPOINT=/api/send-email

# SMTP credentials (server-side only; do NOT expose to client builds)
SMTP_HOST=mail.spacemail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=hello@hifzio.com
SMTP_PASS=your_email_password

# Optional overrides
MAIL_FROM=hello@hifzio.com
MAIL_TO=support@hifzio.com
